[project]
name = "hdf5-subset"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "h5py>=3.16.0",
    "numpy>=2.4.3",
    "pandas>=3.0.1",
]

[dependency-groups]
dev = [
    "ruff>=0.15.6",
]
